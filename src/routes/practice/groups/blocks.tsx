import { GroupSection } from "../group-section";

export default function BlocksGroupPage() {
	return (
		<div className="mx-auto max-w-2xl">
			<GroupSection
				group="blocks"
				subtitle="Phrases, numbers, and time words you reach for every day."
			/>
		</div>
	);
}
